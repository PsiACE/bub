services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bub
    env_file:
      - .env
    volumes:
      - ${BUB_HOME:-${HOME}/.bub}:/data
      - ${BUB_WORKSPACE_PATH:-.}:/workspace
    stdin_open: true
    tty: true
    restart: unless-stopped
